<% if resourse.errors.any? %>
<div style="color: red">
  <h2><%= pluralize(resourse.errors.count, "error") %> prohibited this resourse from being saved:</h2>

  <ul>
    <% resourse.errors.each do |error| %>
      <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>